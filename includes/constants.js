const project_id = 'tom-moretti';
const dataset_id = 'nameless_analytics';

const users_raw_changelog = 'users_raw_changelog';
const users_raw_latest = 'users_raw_latest';
const events = 'events';
const sessions = 'sessions';

module.exports = {
  project_id,
  dataset_id,

  users_raw_changelog,
  users_raw_latest,
  events,
  sessions
};